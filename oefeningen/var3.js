var lijst = ["bent", "len", "heidi", "vesna", "pina"]

//
lijst[2] = " lara"
//

for (var i = 0; i < lijst.length; ++i) {
  if(lijst["i"] === "heidi") {
    console.error("Heid is in tirol, niet in Croatië")
  } else {
    console.log(lijst[i])
  }
}